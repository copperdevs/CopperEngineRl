

using rlMouseButton = Raylib_cs.MouseButton;

namespace CopperEngine.Info;

public enum MouseButton
{
    Left = rlMouseButton.MOUSE_BUTTON_LEFT,
    Right = rlMouseButton.MOUSE_BUTTON_RIGHT,
    Middle = rlMouseButton.MOUSE_BUTTON_MIDDLE,
    Side = rlMouseButton.MOUSE_BUTTON_SIDE,
    Extra = rlMouseButton.MOUSE_BUTTON_EXTRA,
    Forward = rlMouseButton.MOUSE_BUTTON_FORWARD,
    Back = rlMouseButton.MOUSE_BUTTON_BACK,
}